{
  "name": "@txguardian/extension",
  "version": "1.0.0",
  "private": true,
  "description": "TxGuardian browser extension — pre-sign verdict overlay for Solana wallets.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "package": "vite build && node ./scripts/zip-dist.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@txguardian/sdk": "workspace:*",
    "@solana/web3.js": "^1.95.4"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.32",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
